package com.camlauncher.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary palette — electric blue with warm accents
val Primary = Color(0xFF6C63FF)        // Vibrant indigo
val PrimaryVariant = Color(0xFF5A52E0) // Deeper indigo
val Secondary = Color(0xFFFF6B6B)      // Coral red (recording indicator)
val SecondaryVariant = Color(0xFFEE5A5A)

// Surface colors — deep dark theme
val Surface = Color(0xFF121218)         // Near-black with slight blue
val SurfaceVariant = Color(0xFF1E1E2A) // Card backgrounds
val SurfaceElevated = Color(0xFF252535) // Elevated surfaces (dialogs, sheets)
val Background = Color(0xFF0A0A10)      // True background

// Text colors
val OnPrimary = Color.White
val OnSurface = Color(0xFFE8E8F0)      // Primary text
val OnSurfaceVariant = Color(0xFF9999B0) // Secondary text
val OnBackground = Color(0xFFE8E8F0)

// Accent colors
val Success = Color(0xFF4ADE80)
val Warning = Color(0xFFFBBF24)
val Error = Color(0xFFEF4444)

// Recording state colors
val RecordingActive = Color(0xFFFF3B3B)
val RecordingIdle = Color(0xFF4ADE80)
val RecordingPulse = Color(0xFFFF6B6B)

// Surface colors — Light theme
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFF0F0F5)
val SurfaceElevatedLight = Color(0xFFF9F9FC)
val BackgroundLight = Color(0xFFFAFAFF)

// Text colors — Light theme
val OnPrimaryLight = Color.White
val OnSurfaceLight = Color(0xFF1A1A24)
val OnSurfaceVariantLight = Color(0xFF66667A)
val OnBackgroundLight = Color(0xFF1A1A24)
